{
    "scripts": {
        "test": "npx playwright test --headed --reporter=line,allure-playwright",
        "test:report": "npx playwright test --headed --reporter=html",
        "test:headless": "npx playwright test --reporter=html",
        "test:firefox": "npx playwright test --browser=firefox --headed --reporter=html"
    },
    "devDependencies": {
        "@playwright/test": "^1.17.2",
        "allure-playwright": "^2.0.0-beta.14",
        "dotenv": "^11.0.0"
    }
}
